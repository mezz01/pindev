import next from "next/types"
import Image from "next/image";

export default function Card(params:type) {
    let str : string = "amine"
    return (
        <section>
            <h1>{str}</h1>
            <h2></h2>
        </section>
    )
}